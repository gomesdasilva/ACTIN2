ACTIN 2 TODO LIST


MINIMUM REQUIREMENTS:

1. Read spectra and extract required headers from the following spectrographs:
    1. HARPS    []
    2. HARPS-N  []
    3. ESPRESSO []
    4. SPIRou   []


2. Calculate user defined activity indices using s-index method



1. STRUCTURE:

1.1 RaedSpec - Class to read fits files and extract data
    Requirements:
        - Automatically recognizes spectrograph and calls required class
        - Easy to add new spectrographs
        - Easy to add new headers to be extracted

    Returns:
        - spectrum
        - minimum headers (bjd, rv, date, etc)
        - (optional) CCF profile
        - (optional) CCF bisector


1.2 CalcInd - Class to calculate activity indices
    Requires:
        - Spectrum at stellar rest frame (ReadSpec)
            - Wave, Flux deblazed, Flux errors, (optional) RON
        - Indices information (IndTable)

    Returns:
        - Pandas df table with results
        - (optional) Save results to csv, rdb



# TODO:
1. remove "_header" dictionary from code [done for HARPS]
2. Add way to add new headers to be retrieved



Required to correct wave to stellar rest frame:
- rv, berv (depending on the file format)

Required minimum headers:
-------------------------
- instrument id
- file type
- BJD
- actin version?


Required log file:


Required for CalcInd:
---------------------
- wave (stellar rest frame)
- flux (preferably deblazed)
- flux photon noise (preferably from raw flux counts)
    TODO: deal with no photon noise!
- RON (option; dealt with in CalcIndex __init__)
- index parameters (from IndTable class)
